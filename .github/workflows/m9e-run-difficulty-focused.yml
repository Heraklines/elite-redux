name: M9E Captured Run Difficulty
on:
  push:
    branches: [codex/m9e-run-difficulty-focused-20260908]
permissions:
  contents: read
concurrency:
  group: m9e-run-difficulty-${{ github.ref }}
  cancel-in-progress: true
jobs:
  measure:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    env:
      GIT_LFS_SKIP_SMUDGE: '1'
      CARGO_INCREMENTAL: '0'
      CARGO_TERM_COLOR: never
      CARGO_PROFILE_DEV_DEBUG: '0'
      CARGO_PROFILE_DEV_OPT_LEVEL: '0'
      CARGO_PROFILE_DEV_DEBUG_ASSERTIONS: 'true'
      CARGO_PROFILE_DEV_OVERFLOW_CHECKS: 'true'
      CARGO_PROFILE_TEST_DEBUG: '0'
      CARGO_PROFILE_TEST_OPT_LEVEL: '0'
      CARGO_PROFILE_TEST_DEBUG_ASSERTIONS: 'true'
      CARGO_PROFILE_TEST_OVERFLOW_CHECKS: 'true'
      RUSTUP_TOOLCHAIN: '1.97.1'
    steps:
      - name: Start shared deadline before checkout
        run: echo "M9E_STARTED_AT=$(date +%s)" >> "$GITHUB_ENV"
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          persist-credentials: false
          submodules: false
          lfs: false
      - name: Retrieve exact baseline source within shared budget
        timeout-minutes: 10
        shell: bash
        run: |
          set -euo pipefail
          elapsed=$(($(date +%s) - M9E_STARTED_AT))
          test "$elapsed" -ge 0
          remaining=$((1780 - elapsed))
          test "$remaining" -gt 0
          timeout "$((remaining < 600 ? remaining : 600))" git fetch --no-tags --depth=1 origin af3e7824ba7c204a48a5ba9d1123f37736cadd8c
      - name: Restore existing dependency downloads
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: m9e-focused-deps-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust/rust-toolchain.toml', 'rust/Cargo.lock') }}
      - name: Qualify all fifteen natural difficulty and battle tests
        run: python3 scripts/ci/m9e_run_difficulty_diagnostic.py
      - name: Compact run difficulty result
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: m9e-run-difficulty-summary-${{ github.sha }}
          path: ${{ runner.temp }}/m9e-run-difficulty/compact/
          if-no-files-found: error
          retention-days: 14
      - name: Remote proposal diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: m9e-run-difficulty-diagnostics-${{ github.sha }}
          path: ${{ runner.temp }}/m9e-run-difficulty/diagnostics/
          if-no-files-found: error
          retention-days: 14
