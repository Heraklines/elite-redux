name: M9E Type Immunity
on:
  push:
    branches: [codex/m9e-type-immunity-focused-20260908]
permissions:
  contents: read
concurrency:
  group: m9e-type-immunity-${{ github.ref }}
  cancel-in-progress: true
jobs:
  measure:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    env:
      GIT_LFS_SKIP_SMUDGE: '1'
      CARGO_INCREMENTAL: '0'
      CARGO_TERM_COLOR: never
      CARGO_PROFILE_DEV_DEBUG: '0'
      CARGO_PROFILE_DEV_OPT_LEVEL: '0'
      CARGO_PROFILE_DEV_DEBUG_ASSERTIONS: 'true'
      CARGO_PROFILE_DEV_OVERFLOW_CHECKS: 'true'
      CARGO_PROFILE_TEST_DEBUG: '0'
      CARGO_PROFILE_TEST_OPT_LEVEL: '0'
      CARGO_PROFILE_TEST_DEBUG_ASSERTIONS: 'true'
      CARGO_PROFILE_TEST_OVERFLOW_CHECKS: 'true'
      RUSTUP_TOOLCHAIN: '1.97.1'
    steps:
      - name: Start shared deadline before checkout
        run: echo "M9E_STARTED_AT=$(date +%s)" >> "$GITHUB_ENV"
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          persist-credentials: false
          submodules: false
          lfs: false
      - name: Retrieve exact baseline source within shared budget
        timeout-minutes: 10
        shell: bash
        run: |
          set -euo pipefail
          elapsed=$(($(date +%s) - M9E_STARTED_AT))
          test "$elapsed" -ge 0
          remaining=$((1780 - elapsed))
          test "$remaining" -gt 0
          timeout "$((remaining < 600 ? remaining : 600))" git fetch --no-tags --depth=1 origin 2312d794e2cec0cac4d2c91872482ef2e8890ffd
      - name: Restore existing dependency downloads
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: m9e-focused-deps-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust/rust-toolchain.toml', 'rust/Cargo.lock') }}
      - name: Qualify all six whole damage query tests
        run: python3 scripts/ci/m9e_type_immunity_diagnostic.py
      - name: Compact type immunity result
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: m9e-type-immunity-summary-${{ github.sha }}
          path: ${{ runner.temp }}/m9e-type-immunity/compact/
          if-no-files-found: error
          retention-days: 14
      - name: Remote proposal diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: m9e-type-immunity-diagnostics-${{ github.sha }}
          path: ${{ runner.temp }}/m9e-type-immunity/diagnostics/
          if-no-files-found: error
          retention-days: 14
