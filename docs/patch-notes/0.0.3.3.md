# Elite Redux 0.0.3.3

Crash fixes and early-game balance.

## 🐛 Bug Fixes

- **Challenge runs no longer black-screen.** Starting a challenge (e.g. a **Mono-type** run) and leaving the challenge-select screen blacked out the starter screen. Building the starter grid under a challenge crashed on certain species whose form data was incomplete (many Elite Redux customs / evolution targets), aborting the whole screen. The grid now handles those species safely, so challenge runs start normally. *(Tested on PC & mobile.)*
- **Megas no longer appear in the early game.** Elite Redux trainers could field a Mega-evolved Pokémon very early (notably in Ace), because a mon holding a Mega Stone always Mega-evolved regardless of level. Mega evolution now only happens once the Pokémon is **level 60+**, so megas show up as the late-game power spike they're meant to be (and naturally a bit sooner on Hell, where levels climb faster).

## 🎨 Known issue

- **Floette (Eternal Flower)** has a blue box behind its sprite — the sprite art itself has a baked-in background that needs to be made transparent in the asset pack. Tracked for an art fix.

---

_Reminder: clearing your browser's "site data" will wipe local saves — please don't. Use **Manage Data → Export** to back up._
