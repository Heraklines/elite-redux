# Current ordinary damage query

`er_battle::m7_resolver::query_simulated_move_damage_v5` is an immutable query over prepared current battle content and a validated run. It accepts the source field slot, move slot and target field slot. It validates active occupancy and remaining PP using PP Ups and overrides before calculating damage. Status or no-power moves return zero.

The query and real turn execution share the damage calculation body. Queries use a fixed100-percent variance and noncritical calculation, with no RNG handle, accuracy roll, move hooks, PP consumption, HP mutation or emitted effects. Real turns still request85..100 variance lazily at the same point in the calculation, preserving their draw order. This extraction does not route the current authority AI through a new scoring policy.

The four source witnesses use the existing prepared content and completed bootstrap constructor. Controlled current state pins actual Cut and Confusion definitions at equal power50, neutral typing and independently chosen physical/special defenses. The expected damage46/13 reverses when defenses swap. A separate real turn before and after reordered queries must have identical full transition and RNG audit, including independent accuracy, critical and consuming variance expectations. PP Up/override boundaries and zero/inactive rejection cases conserve the complete state. These are controlled domain witnesses, not natural whole-game journey evidence.

The supported result follows the current resolver's existing semantics. Its minimum-one damage floor also preserves the current immunity limitation. This cut does not repair fixed damage, OHKO, stages, forced critical behavior, defender or ally ability knowledge, spread effects, complete source rounding, AI profiles or move scoring. Do not describe the result as full source-equivalent AI simulation.

The exact resolver/query-test change requires complete reverse compilation and selected-package Clippy plus causal inventories and both timer/replica behavioral controls. It adds all battle and AI execution and named non-retention game targets, with50 required target identities. Existing V7 material-retention witnesses, actual Wasm and Worker/in-page Chromium, typed/codec witnesses and normal CLI replay bridge remain required. The long save/delete helper remains compiled; its dedicated execution policy is unchanged for retention changes.

The larger native proof uses lossless compressed ID transport only when the old indexed form exceeds64KiB. Every full test ID, target identity, ordering, partition, required-ID permutation and semantic hash is restored and validated. Decompression is bounded before JSON parsing, and the existing128KiB expanded proof cap remains. Projection estimates are not qualification evidence; only actual remote output can certify the candidate.
