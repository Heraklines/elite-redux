# M8 TypeScript/Rust shadow parity

TypeScript is sole authority. One normalized external event stream feeds TypeScript and Rust in identical sequence. Rust effects are quarantined and cannot call UI, storage, network, audio, assets, lifecycle, or telemetry adapters.

Bootstrap modes are natural seed/selections, migrated TypeScript save, and stable canonical boundary. Mid-transaction attachment is forbidden.

Both sides emit `CommonGameProjectionV1`: game, control, RNG, last material, semantic save, and presentation. Comparison occurs after bootstrap, control installation, settled turn/replacement/interaction/progression/wave/capture/scenario/save/terminal.

Results are Equal, PresentationOnlyDifference, PlatformOnlyDifference, MechanicalDivergence, and UnsupportedComparison. UnsupportedComparison cannot satisfy G41.

The first mechanical divergence stops shadow for the session and exports one deduplicated `DualRuntimeReproCapsuleV1` containing browser identity, shared external trace, TypeScript snapshot, Rust capsule, hierarchical path, causal source, and optional render trace. Later equal fingerprints increment count only.

G41 requires every frozen current mode and M7.2 regression capsule, 10,000+ worker traces, selected rendered complete journeys, zero unexplained mechanical divergence, and bounded overhead/teardown.
